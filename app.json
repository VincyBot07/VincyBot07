{
  "name": "VincyBot07",
  "description": "Il bot di Vincysuper07!",
  "repository": "https://github.com/kyb3r/modmail",
  "env": {
    "TOKEN": {
      "description": "Il token del tuo bot.",
      "required": true
    },
    "GUILD_ID": {
      "description": "L'ID del server in cui stai hostando questo bot.",
      "required": true
    },
    "OWNERS": {
      "description": "ID proprietari (separati da virgola) per usare i comandi per solo i proprietari come eval.",
      "required": true
    },
    "MONGO_URI": {
      "description": "Il Database MongoDB dove il bot metterà gran parte dei dati.",
      "required": true
    },
    "LOG_URL": {
      "description": "L'URL del logviewer dove ci saranno i log delle stanze chiuse.",
      "required": true
    }
  }
}
